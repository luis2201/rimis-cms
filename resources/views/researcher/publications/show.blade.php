<x-app-layout><x-slot name="header"><h1>{{ $publication->title }}</h1></x-slot><div class="row"><div class="col-md-8"><div class="card"><div class="card-body"><p><strong>{{ $publication->publicationTypeLabel() }}</strong></p><p>{{ $publication->abstract }}</p><p><strong>Autores:</strong> {{ $publication->authors->pluck('author_name')->join(', ') }}</p><p><strong>DOI:</strong> {{ $publication->doi ?: '—' }} · <strong>Enlace:</strong> {{ $publication->external_url ?: '—' }}</p>@if($publication->hasPdf())<a class="btn btn-outline-danger" href="{{ route('researcher.publications.pdf',$publication) }}">Descargar mi PDF</a>@endif</div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><p><strong>Revisión:</strong> {{ $publication->reviewStatusLabel() }}</p><p><strong>Publicación:</strong> {{ $publication->isPublished()?'Publicada':($publication->isApproved()?'Aprobada — pendiente de publicación':'Borrador') }}</p>@if($publication->review_notes)<div class="alert alert-warning">{{ $publication->review_notes }}</div>@endif @if($publication->isEditableByResearcher())<a class="btn btn-primary btn-block" href="{{ route('researcher.publications.edit',$publication) }}">Editar</a><form method="POST" action="{{ route('researcher.publications.submit',$publication) }}" class="mt-2">@csrf<button class="btn btn-success btn-block">Enviar a revisión</button></form>@endif @if($publication->isPublished())<a class="btn btn-outline-primary btn-block" href="{{ route('research-publications.show',$publication->slug) }}">Ver ficha pública</a>@endif</div></div></div></div></x-app-layout>
