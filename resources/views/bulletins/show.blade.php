<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $bulletin->title }} | RIMIS</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="bg-slate-50 font-sans text-slate-800">
    <x-public-header :menus="$publicMenus" />
    <main class="mx-auto grid max-w-5xl gap-10 px-5 py-12 md:grid-cols-[300px_1fr]">@if($bulletin->coverImage)<img src="{{ $bulletin->coverImage->publicUrl() }}" class="w-full rounded-xl object-cover shadow-lg" alt="{{ $bulletin->title }}">@else<div class="flex aspect-[3/4] items-center justify-center rounded-xl bg-[var(--rimis-charcoal)] shadow-lg"><i class="fa-solid fa-file-pdf text-7xl text-[var(--rimis-primary)]"></i></div>@endif<article><span class="text-sm font-bold uppercase tracking-wider text-[var(--rimis-primary-dark)]">Boletín · {{ $bulletin->published_at->format('d/m/Y') }}</span><h1 class="mt-3 text-4xl font-bold text-slate-900">{{ $bulletin->title }}</h1>@if($bulletin->description)<p class="mt-6 whitespace-pre-line text-lg leading-8 text-slate-600">{{ $bulletin->description }}</p>@endif<div class="mt-8 rounded-xl border border-slate-200 bg-white p-5"><div class="flex flex-wrap items-center justify-between gap-4"><div><strong class="block">{{ $bulletin->pdf_original_name }}</strong><small class="text-slate-500">{{ $bulletin->formattedSize() }}</small></div><a href="{{ route('bulletins.download', $bulletin) }}" class="rounded-lg bg-[var(--rimis-primary)] px-5 py-3 font-semibold text-white hover:bg-[var(--rimis-primary-dark)]"><i class="fa-solid fa-download mr-2"></i>Descargar PDF</a></div></div></article></main>
</body></html>
