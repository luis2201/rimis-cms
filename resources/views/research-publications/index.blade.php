<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Investigaciones | RIMIS</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="bg-slate-50 text-slate-800">@include('public.partials.nav')<main class="mx-auto max-w-7xl px-6 py-12"><h1 class="text-4xl font-bold">Repositorio de investigaciones</h1><form class="mt-8 grid gap-3 md:grid-cols-5"><input name="search" value="{{ request('search') }}" class="rounded border p-3" placeholder="Buscar"><select name="type" class="rounded border p-3"><option value="">Todos los tipos</option>@foreach(\App\Models\ResearchPublication::TYPE_LABELS as $v=>$l)<option value="{{ $v }}" @selected(request('type')===$v)>{{ $l }}</option>@endforeach</select><input name="year" value="{{ request('year') }}" class="rounded border p-3" placeholder="Año"><input name="research_area" value="{{ request('research_area') }}" class="rounded border p-3" placeholder="Área"><button class="rounded bg-teal-700 text-white">Filtrar</button></form><div class="mt-10 grid gap-6 md:grid-cols-3">@forelse($publications as $p)<article class="rounded-xl bg-white p-6 shadow"><span class="text-sm text-teal-700">{{ $p->publicationTypeLabel() }} · {{ $p->year }}</span><h2 class="mt-2 text-xl font-bold"><a href="{{ route('research-publications.show',$p->slug) }}">{{ $p->title }}</a></h2><p class="mt-2 text-sm">{{ $p->authors->pluck('author_name')->join(', ') }}</p><p class="mt-4 text-slate-600">{{ \Illuminate\Support\Str::limit($p->abstract,180) }}</p></article>@empty<p>No se encontraron investigaciones.</p>@endforelse</div><div class="mt-8">{{ $publications->links() }}</div></main>@include('public.partials.footer')</body></html>
