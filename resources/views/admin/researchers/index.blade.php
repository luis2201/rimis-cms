<x-app-layout><x-slot name="header"><h1>Investigadores</h1></x-slot><div class="card"><div class="card-header"><form class="form-inline"><input class="form-control mr-2" name="search" value="{{ request('search') }}" placeholder="Nombre o correo"><select class="form-control mr-2" name="profile_public"><option value="">Todos</option><option value="1" @selected(request('profile_public')==='1')>Públicos</option><option value="0" @selected(request('profile_public')==='0')>Ocultos</option></select><button class="btn btn-primary">Filtrar</button></form></div><div class="card-body table-responsive"><table class="table"><thead><tr><th>Nombre</th><th>Institución</th><th>País / área</th><th>Usuario</th><th>Membresía</th><th>Perfil</th><th>Acciones</th></tr></thead><tbody>@foreach($profiles as $profile)<tr><td>{{ $profile->user->name }}<br><small>{{ $profile->user->email }}</small></td><td>{{ $profile->institution }}</td><td>{{ $profile->country }}<br>{{ $profile->research_area }}</td><td>{{ $profile->user->is_active?'Activo':'Inactivo' }}</td><td>{{ $profile->user->researcherApplication?->status ?? 'Sin postulación' }}</td><td>{{ $profile->profile_public?'Público':'Oculto' }}</td><td><a class="btn btn-sm btn-primary" href="{{ route('admin.researchers.edit',$profile) }}">Editar</a>@if($profile->profile_public)<a class="btn btn-sm btn-outline-info" href="{{ route('researchers.show',$profile->public_slug) }}">Ver</a>@endif<form class="d-inline" method="POST" action="{{ route('admin.researchers.visibility',$profile) }}">@csrf @method('PATCH')<input type="hidden" name="visible" value="{{ $profile->profile_public?0:1 }}"><button class="btn btn-sm {{ $profile->profile_public?'btn-warning':'btn-success' }}">{{ $profile->profile_public?'Ocultar':'Habilitar' }}</button></form></td></tr>@endforeach</tbody></table>{{ $profiles->links() }}</div></div></x-app-layout>
